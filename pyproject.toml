[project]
name = "investment-risk-analysis"
version = "0.1.0"
description = "Investment risk workflow: question to cited risk memo."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
investment-risk = "app.main:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
